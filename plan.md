1. **Identify the UX/Accessibility improvement:** Add `aria-expanded` attributes to buttons that control the visibility of content. In `components/accounts/ppp-savings-panel.tsx`, the expand/collapse button for the PPP savings report does not communicate its expanded state to screen readers. In `components/layout/mobile-navigation-menu.tsx`, the menu toggle button also does not have an `aria-expanded` attribute. This is a crucial accessibility fix for keyboard and screen reader users navigating collapsible elements.
2. **Implement in `components/accounts/ppp-savings-panel.tsx`:** Find the button with `onClick={() => setCollapsed((value) => !value)}` and add `aria-expanded={!collapsed}`. Wait, if it's collapsed, expanded is false. So `aria-expanded={!collapsed}`. Wait, is there a way to add aria-controls? Probably not necessary unless there's an ID, but `aria-expanded` alone is a big win.
3. **Implement in `components/layout/mobile-navigation-menu.tsx`:** Find the button with `onClick={() => setOpen(true)}` and add `aria-expanded={open}`. Note that the menu overlay itself has a close button, but the button that opens it should indicate whether it's expanded or not. Actually, since the button is conditionally rendered with `md:hidden`, when it's open, is the button still there? Ah, let's check. Yes, it's outside the `{open ? ... : null}` block. So add `aria-expanded={open}`.
4. **Log critical learning in `.Jules/palette.md`:** Following the memory rule, I will log an entry explaining why `aria-expanded` is critical for collapsible components, if this specific pattern was noticed across multiple files.
5. **Run tests:** Run `npm run typecheck` and `npx oxlint app components lib scripts --deny-warnings` to ensure the build checks pass.
6. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
7. **Submit the PR:** Create a PR titled `🎨 Palette: [UX improvement]` with the required format.
