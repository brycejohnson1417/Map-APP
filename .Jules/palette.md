## 2024-05-21 - Added aria-expanded to ChangeRequestList\n**Learning:** Expandable accordion components should always link their trigger `<button>` to the expanded content container `<div>` using matching `aria-controls` and `id`, and provide dynamic `aria-expanded` attributes on the button to properly inform screen reader users of state changes.\n**Action:** Will ensure all future expandable sections implement this linked ID pattern and toggle state.
